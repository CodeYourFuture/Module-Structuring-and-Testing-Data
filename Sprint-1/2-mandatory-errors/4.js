const twelveHourClockTime = "8:53pm";
const twentyFourHourClockTime = "20:53";


/*
The error occurs because variable names cannot start with a number.

The variable name 12HourClockTime begins with 12, which is not valid JavaScript syntax,
causing a SyntaxError.

Renaming the variables so they start with letters fixes the issue.
*/
