let count = 0;

count = count + 1;
// Line 1 is a variable declaration, creating the count variable with an initial value of 0
// Describe what line 3 is doing, in particular focus on what = is doing
//line 3 increments the current value of the variable 'count' by 1.
//The '=' operator assigns the new value to 'count', updating its previous value.