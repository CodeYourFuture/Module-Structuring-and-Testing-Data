let count = 0;

// count = count + 1/; the line 3 is showing the incrementation by the count of 1. It updates the value of the count by adding 1 to its declared value
count++;
console.log(count);
// Line 1 is a variable declaration, creating the count variable with an initial value of 0
// Describe what line 3 is doing, in particular focus on what = is doing
