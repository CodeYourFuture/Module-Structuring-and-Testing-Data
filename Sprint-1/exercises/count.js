let count = 0;

count = count + 1;

// Line 1 is a variable declaration, creating the count variable with an initial value of 0
// Describe what line 3 is doing, in particular focus on what = is doing

The variable named "count" is being assigned a new value by the =, where whatever the value of "count" is will be changed by the addition of 1.