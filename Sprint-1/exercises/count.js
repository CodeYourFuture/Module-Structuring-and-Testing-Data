const count = 0;

count = count + 1;


// Line 1 is a variable declaration, creating the count variable with an initial value of 0
// Describe what line 3 is doing, in particular focus on what = is doing

//Answer:
//we reassigning the count variable to a new value which is count+1
//here = is assignment operator which takes the value on the right side (count + 1) and assigns it to the variable on the left side (count).

