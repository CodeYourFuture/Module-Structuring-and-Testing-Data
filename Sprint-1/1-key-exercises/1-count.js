let count = 0;

count = count + 1;

// Line 1 is a variable declaration, creating the count variable with an initial value of 0
// Describe what line 3 is doing, in particular focus on what = is doing


// Line 3 takes the current value of count, adds 1 to it,
// and assigns the new value back to count.
//
// The = sign is assignment, not equality.
// It means "store the result on the right into the variable on the left".
//
// So:
// count starts as 0
// count + 1 becomes 1
// count is updated to 1