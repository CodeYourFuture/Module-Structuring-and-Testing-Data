let count = 0;

count = count + 1;

// Line 1 is a variable declaration, creating the count variable with an initial value of 0
// Describe what line 3 is doing, in particular focus on what = is doing

// the value stored in count is 0 to start with
// With the code in line 3 we add one to the value that is stored in count (RHS) to make it 1.
// this is then reassign to count, so the updated  value that is stored in count is 1 now.
