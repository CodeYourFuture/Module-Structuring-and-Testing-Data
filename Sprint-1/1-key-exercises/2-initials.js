let firstName = "Creola";
let middleName = "Katherine";
let lastName = "Johnson";
let index=0;

// Declare a variable called initials that stores the first character of each string.
// This should produce the string "CKJ", but you must not write the characters C, K, or J in the code of your solution.

let initials = ``;
//let initials = firstName.charAt(index)+middleName.charAt(index)+lastName.charAt(index);
//we can write just one row to find the solution. Declare and assignment
initials = firstName.charAt(index)+middleName.charAt(index)+lastName.charAt(index);
console.log(initials);

// https://www.google.com/search?q=get+first+character+of+string+mdn

