// Predict and explain first...
//The functions logs the result to the console. There will be an error when trying to log within a console.log

function multiply(a, b) {
  return (a * b);
}

console.log(`The result of multiplying 10 and 32 is ${multiply(10, 32)}`);

//I fixed it by returning the result, instead of logging it