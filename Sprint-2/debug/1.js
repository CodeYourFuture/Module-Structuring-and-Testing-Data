// Predict and explain first...

function sum(a, b) {
  return a + b; //remove semicolomn, as it terminate the function, so a +b doesn't execute. after return and move up a+b, to return the result of addition. 
}

console.log(`The sum of 10 and 32 is ${sum(10, 32)}`); // the final string will be The sum of 10 and 32 is undefined
