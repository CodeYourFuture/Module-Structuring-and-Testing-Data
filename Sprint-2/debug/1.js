// Predict and explain first...
//returns undefined because a + b expression is not in the same line as return statement.

function sum(a, b) {
  return a + b;
}

console.log(`The sum of 10 and 32 is ${sum(10, 32)}`);
