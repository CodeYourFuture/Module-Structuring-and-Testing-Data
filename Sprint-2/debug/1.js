// Predict and explain first...

function sum(a, b) {
  return;
  a + b;
}

console.log(`The sum of 10 and 32 is ${sum(10, 32)}`);
// The console logs "The sum of 10 and 32 is" because the function does not return any value.
// The return; causes the function to exit immediately. Therefore, the line a+b is never executed.