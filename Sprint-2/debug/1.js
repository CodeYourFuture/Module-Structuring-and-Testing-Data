// Predict and explain first...
// The code has a syntax error after the return the word, it will give an error when we try to run it.
// To fix it I will remove the semi-colon and place the a + b in front of the return statement

function sum(a, b) {
  return a + b;
}

console.log(`The sum of 10 and 32 is ${sum(10, 32)}`);
