// Predict and explain first...

function sum(a, b) {
  return;
  a + b;
}

console.log(`The sum of 10 and 32 is ${sum(10, 32)}`);

// console.log calls the function sum with the two requiring parameters, and it then returns undefined. The return statement is used to return a value, but because there is no value after the return statement it then returns undefined.
