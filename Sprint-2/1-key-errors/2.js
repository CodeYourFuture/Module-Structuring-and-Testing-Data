// Predict and explain first BEFORE you run any code...

// this function should square any number but instead we're going to get an error

// =============> The code will produce an error because the function parameters must be variable names, instead of a value - in this case the error has occured because of 3.

// function square(3) {
//     return num * num;
// }

// =============> SyntaxError: Unexpected number

// =============> The error message suggests that JavaScript requires a variable name rather than a number.

// Finally, correct the code to fix the problem

// =============>
function square(num) {
  return num * num;
}
