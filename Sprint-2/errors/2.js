// Predict and explain first...

// this function should square any number but instead we're going to get an error

//function square(3) {
function square(num) {
  return num * num;
}

console.log(`${square(5)}`);
//In line 6, we've got syntax error: unexpected token, which means somethimg is wrong with the syntax.
//and yes, it is.when we define a function we have to use parameter or in another language we have to use a variable.
//so 3 is not a parameter. I used line 7 instead
